export class Coach {
    idCoach: number;
    nameCoach: string;
    email: string;
    rapport: string; // Ensure this matches the case and structure expected from the backend
    clubName: string;
    // other properties...
  }
  